Files descriptions:

- Db4oAnalyzer.Test.dll: has some entities just for test of the application only.
- previous_session_code: it's a text file that contain some lines of example.
- app.config: has the configuration of the aplication (very important).
